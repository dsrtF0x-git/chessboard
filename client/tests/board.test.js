import { Board } from "../scripts/board.js";
// const board = new Board(true);

// test("Check if all pieces in place", () => {
//   expect(board.grid[1].every(item => item.name === "Pawn")).toEqual(true);
// });

test("adsadsa", () => {
  expect(1+3).toBe(4);
});